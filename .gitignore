node_modules/
.env
escalations.json
conversations.json
chat-history/extracted/
# Real exported WhatsApp chats (PII). Kept locally / in secure storage for KB
# tuning only — never tracked in git.
chat-history/*.zip
